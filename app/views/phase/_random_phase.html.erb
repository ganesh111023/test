
<%- session[:fetched_record].each do |p| %>
	<div class="row" id= "<%= @phase.id%>">
		<div class="col-md-12 text-center" id= "<%= @phase.id%>">
			sample phrase number <%= p %>
		</div>
	</div>
<% end %>